// Take float input and print the fractional part of the real number.
#include<stdio.h>
int main() {
    // float deci;
    // printf("Enter a decimal number: ");
    // scanf("%f", &deci);
    // int inte = deci;
    // float frac = deci - inte;
    // printf("Fractional part of decimal you entered is: %f", frac);
    // int i = 2, j = 3, k, l;
    // float a, b;
    // k = (i / j) * j;
    // l = (j / i) * i;
    // a = (i / j) * j;
    // b = (j / i) * i;
    // printf("%d %d %f %f", k, l, a, b);
    float a = 22, b = 7;
    printf("%f", a/b);
    return 0;
}