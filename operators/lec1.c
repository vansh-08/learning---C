/*  #include <stdio.h>
int main(){
    //printf("Hello World");
    int x = 5;
    printf("%d",x);
    x = 7;
    printf("%d",x);
    x = x + 6;
    printf("%d",x);
    x = x - 20;
    printf("%d",x); 
    return 0;
}*/


// WAP(Write a program) to display area of circle with given radius.
#include <stdio.h>
int main(){
    // int r = 5;
    // float pi = 3.1413;
    // float area = pi * r * r;
    // printf("Area of circle: %.2f\n", area);

// Print the percentage of 4 subjects where marks are out of 40
    float math = 30;
    float bee = 34;
    float python = 36;
    float c = 38;
    float percentage = ((math + bee + python + c) * 5) / 8;
    printf("Percentage : %.2f\n", percentage);     
    return 0;
    }
